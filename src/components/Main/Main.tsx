import "./Main.scss";

const Main = () => {
  return (
    <main>
      <section>
        <input type="text" />
        <select>{}</select>
      </section>
      {}
      <button>läs in fler</button>
    </main>
  );
};

export default Main;
